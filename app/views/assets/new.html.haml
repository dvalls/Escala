%h1 Assets#new
%p Find me in app/views/Assets/new.html.haml

= render partial: 'form', locals: {:asset => @asset, :subcategories => @subcategories}


= link_to 'Cursos', courses_path


-#= simple_form_for(@asset) do |f|
-#  = f.error_notification
-#
-#  .form-inputs
-#    = f.input :name
-#    = f.input :description
-#    = f.input :format
-#    = f.input :url
-#    = f.input :thumbnail
-#    = f.input :category_id, label: 'Categoria', :collection => @subcategories, value_method: :id, label_method: :name, as: :radio_buttons
-#
-#  .form-actions
-#    = f.button :submit, 'CRIAR Material, ou coisa', :class => 'btn btn-primary'
-#
