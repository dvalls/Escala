%h3 PAGES#INDEX

%h3 Pages
%table.table
  %tr
    %th Pagina
    %th Curso
    %th Blocos
    %th
    %th
  - @pages.each do |page|
    %tr
      %td= page.name
      %td= page.course.title
      %td
        - page.content_blocks.each do |block|
          = '-'
          = block.order
          = block.contentable.name
          = '/'
          = block.contentable.class.name
          %br/
      %td= link_to 'ADD BLOCO', new_page_content_block_path(page)
      %td
        = link_to 'EDITAR', edit_page_path(page)
        |
        = link_to 'APAGAR', page, method: :delete, :data => {:confirm => 'Tem certeza?'}

= link_to 'nova pagina', new_page_path, class: 'btn btn-primary' if admin_logged?

%h3 Blocos de conteudos
%p
  = render partial: 'content_blocks/index'
.row
  .col-lg-4.col-md-4
    %h3 Grupos (videos)
    = render partial: 'content_video_groups/index'
  .col-lg-4.col-md-4
    %h3 Grupos (arquivos de biblioteca)
    = render partial: 'content_library_groups/index'
  .col-lg-4.col-md-4
    %h3 Grupos (documentos)
    = render partial:  'content_docs_groups/index'
  .clearfix.hidden-sm-up

= link_to 'voltar', root_path if admin_logged?



-#.row
-#  .col-lg-3.col-md-3
-#    %ul
-#      - @pages.each do |page|
-#        %li= link_to page.name, page_content_blocks_path(page)
-#  .col-lg-9.col-md-9
-#    %h4 diahdsiahsuidhuiashiudhaishid
-#    %h4 Say woot?

    -#= render content_blocks: 'content_blocks/index'
