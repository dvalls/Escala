
-content_for :navbar do
  .pull-left= link_to image_tag(ENV['LOGO_URL']), my_course_index_path

  .navbar-header
    %button.navbar-toggle{"data-target" => "#menu", "data-toggle" => "collapse", type: "button"}
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar

  #menu.collapse.navbar-collapse
    %nav
      %ul.nav.navbar-nav
        %li= link_to 'Videos', '#'
        %li
        %li
          - case session[:user_id]
            - when nil
              = link_to 'Login', access_login_path
            - else
              = link_to 'Perfil', my_course_student_path(session[:user])
              %li
                = link_to 'curso(s)', my_course_index_path
              %li
                = link_to 'Logout', access_logout_path

