%table.table
  %tr
    %th Nome
    %th
  - @docs_groups.each do |group|
    %tr
      %td= group.name
      %td
        = link_to 'edit', edit_content_docs_group_path(group)
        |
        = link_to 'delete', group, method: :delete, :data => { :confirm => 'Você tem certeza disso?'}


= link_to 'novo grupo', new_content_docs_group_path, class: 'btn btn-primary'


= link_to ' voltar', root_path
