/ Carousel
#carousel.carousel.slide
  %ol.carousel-indicators
    - @carousels.each_with_index do |carousel, index|
      %li{:class => (index == 0 ? 'active' : ''), "data-target" => "#carousel", "data-slide-to" => "#{index}"}

  / Carousel slides
  .carousel-inner
    - @carousels.each_with_index do |carousel, index|
      .item{:class => (index == 0 ? 'active' : '')}
        =image_tag carousel.image_url

        - unless carousel.title.empty?
          = link_to carousel.course do
            .carousel-caption
              = carousel.title + carousel.description
/ End Carousel

= link_to 'editar galeria', carousels_path, :class => 'btn btn-primary' if admin_logged?
%br/
= link_to 'videos', content_videos_path, :class => 'btn btn-primary' if admin_logged?
= link_to 'arquivos de biblioteca', library_files_path, :class => 'btn btn-primary' if admin_logged?
= link_to 'documentos', docs_path, :class => 'btn btn-primary' if admin_logged?
%br/
= link_to 'alunos', students_path, :class => 'btn btn-primary' if admin_logged?
= link_to 'categorias', categories_path, :class => 'btn btn-primary' if admin_logged?
= link_to 'pages', pages_path, class: 'btn btn-primary' if admin_logged?

/ Feeds
%ul.feeds.list-unstyled
  - @feeds.each do |feed|
    %li
      %h5
        %b= feed.title
        |
        =l feed.updated_at.to_date, format: :default
      = image_tag feed.image.url(:thumb) unless feed.image.blank?
      %p= feed.body
/ End Feeds



=link_to t('views.all_news'), feeds_path

%br/


= stylesheet_link_tag "home"
= javascript_include_tag "home"
