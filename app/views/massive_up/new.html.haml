%h1 MassiveUp#new
%p Find me in app/views/massive_up/new.html.haml
= simple_form_for( @archive, :url => massive_up_path, :method => :post, :html => {:multipart => true}) do |f|
  = f.error_notification

  .form-inputs
    = f.input :url, as: :file, :input_html => { :multiple => true }
    = f.input :description
    = hidden_field_tag :description, :description

    = f.input :category_id, label: 'Categoria', :collection => Category.all, value_method: :id,
              label_method: :name, as: :radio_buttons
    = hidden_field_tag :category_id, :category_id

    = f.input :course_id, collection: Course.all, value_method: :id, label_method: :title, as: :radio_buttons
    = hidden_field_tag :course_id, :course_id

  %p
    É possível selecionar uma ou mais imagens.

  .form-actions
    = f.button :submit, 'SALVAR',  :class => 'btn btn-primary'